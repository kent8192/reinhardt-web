meta {
  name: Create Snippet - Missing Title (400)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/snippets/
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "code": "fn main() {}",
    "language": "rust"
  }
}

tests {
  test("Status code is 400 or 422", function() {
    expect([400, 422]).to.include(res.status);
  });
}
