# Production environment settings for tutorial-rest example
# Settings for production deployment

debug = false

# Production secret key (MUST use environment variable)
secret_key = "production-secret-key-MUST-use-env-var"

# Restrictive allowed hosts for production
allowed_hosts = ["api.example.com", "www.example.com"]

# Disable template debug in production
template_debug = false

# Production database configuration (override all via environment variables)
[database]
engine = "sqlite"
name = "db.sqlite3"
