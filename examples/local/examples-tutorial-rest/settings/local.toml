# Local development settings for tutorial-rest example
# Override base settings for local development

debug = true

# Local development secret key (DO NOT use in production)
secret_key = "local-dev-secret-key-change-this-in-production"

# Local database configuration (can override DATABASE_URL via environment)
[database]
engine = "postgresql"
host = "localhost"
port = 5432
name = "snippets_db"
user = "postgres"
password = "postgres"
