# Production environment settings for tutorial-basis example
# Settings for production deployment

debug = false

# Production secret key (MUST use environment variable)
secret_key = "production-secret-key-MUST-use-env-var"

# Restrictive allowed hosts for production
allowed_hosts = ["example.com", "www.example.com"]

# Disable pages debug in production (reinhardt-pages)
template_debug = false
