# Copy this file to settings/local.toml for local development.
debug = true
secret_key = "change-me-in-production"

# LM Studio endpoint is configured via environment variables (not TOML)
# so that operators can override it without rebuilding the example:
#
#   LM_STUDIO_URL         (default: http://localhost:1234/v1/chat/completions)
#   LM_STUDIO_MODEL       (default: glm-edge-v-5b)
#   LM_STUDIO_MAX_TOKENS  (default: 1800)
