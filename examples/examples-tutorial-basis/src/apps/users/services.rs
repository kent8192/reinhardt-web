//! Server-side services for the users app.
//!
//! Authentication logic currently lives in `models::manager` and
//! `server_fn`. Move cross-cutting user operations here when they need to be
//! shared by more than one handler.
