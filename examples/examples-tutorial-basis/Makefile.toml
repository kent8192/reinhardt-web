# Makefile.toml for Reinhardt Project
#
# This file provides cargo-make task definitions for common development operations.
# Install cargo-make: `cargo install cargo-make`
# Usage: `cargo make <task>`

[config]
default_to_workspace = false
skip_core_tasks = true

# ============================================================================
# Environment Variables
# ============================================================================

[env]
# Use local target directory for each example (independent from workspace)
CARGO_TARGET_DIR = { value = "target", condition = { env_not_set = ["CARGO_TARGET_DIR"] } }

# ============================================================================
# Infrastructure (disposable PostgreSQL + Redis containers)
# ============================================================================
#
# These tasks mirror the `infra-up` / `infra-down` / `infra-reset` shape
# from `reinhardt-cloud/dashboard/Makefile.toml` so the developer
# experience is uniform across the Reinhardt family. The actual provisioning
# is delegated to `scripts/infra_up.sh` (Docker required); credentials are
# parsed from `settings/<profile>.toml`. `runserver` and `dev` chain
# `migrate` (which itself depends on `infra-up`), so a contributor can
# bootstrap the full local stack with a single `cargo make runserver`.

[tasks.infra-up]
description = "Start disposable PostgreSQL + Redis containers (docker run --rm); credentials parsed from settings/<profile>.toml"
command = "bash"
args = ["scripts/infra_up.sh"]
env = { REINHARDT_ENV = "local" }

[tasks.infra-down]
description = "Stop disposable PostgreSQL + Redis containers (data is wiped)"
command = "bash"
args = ["scripts/infra_down.sh"]
env = { REINHARDT_ENV = "local" }

[tasks.infra-reset]
description = "Stop and recreate disposable infrastructure (wipes all data)"
dependencies = ["infra-down", "infra-up"]

# ============================================================================
# Development Server
# ============================================================================

[tasks.runserver]
description = "Start the development server (auto-reloads on changes). Forwards extra args, e.g. `cargo make runserver -- -vv` for verbose output. Auto-starts PostgreSQL/Redis via `infra-up` (transitively through `migrate`)."
dependencies = ["migrate"]
command = "cargo"
args = ["run", "--bin", "manage", "--", "runserver", "--with-pages", "${@}"]

# ============================================================================
# Database Migrations
# ============================================================================

[tasks.makemigrations]
description = "Create new migrations based on model changes"
command = "cargo"
args = ["run", "--bin", "manage", "makemigrations"]

[tasks.migrate]
description = "Apply database migrations (auto-starts PostgreSQL/Redis via `infra-up`)"
dependencies = ["infra-up"]
command = "cargo"
args = ["run", "--bin", "manage", "migrate"]

# ============================================================================
# Static Files
# ============================================================================

[tasks.collectstatic]
description = "Collect static files into STATIC_ROOT"
command = "cargo"
args = ["run", "--bin", "manage", "collectstatic"]

# ============================================================================
# Project Management
# ============================================================================

[tasks.check]
description = "Check the project for common issues"
command = "cargo"
args = ["run", "--bin", "manage", "check"]

[tasks.showurls]
description = "Display all registered URL patterns"
command = "cargo"
args = ["run", "--bin", "manage", "showurls"]

[tasks.shell]
description = "Run an interactive Rust shell (REPL)"
command = "cargo"
args = ["run", "--bin", "manage", "shell"]

# ============================================================================
# Testing
# ============================================================================

[tasks.test]
description = "Run all tests"
command = "cargo"
args = ["nextest", "run", "--all-features"]

[tasks.test-unit]
description = "Run unit tests only"
command = "cargo"
args = ["nextest", "run", "--lib", "--all-features"]

[tasks.test-integration]
description = "Run integration tests only"
command = "cargo"
args = ["nextest", "run", "--test", "*", "--all-features"]

# ============================================================================
# Code Quality
# ============================================================================

[tasks.fmt-check]
description = "Check code formatting (rustfmt + page! DSL)"
command = "reinhardt-admin"
args = ["fmt", ".", "--check"]

[tasks.fmt-fix]
description = "Fix code formatting (rustfmt + page! DSL)"
command = "reinhardt-admin"
args = ["fmt", "."]

[tasks.clippy-check]
description = "Check linting rules"
command = "cargo"
args = ["clippy", "--all-features", "--", "-D", "warnings"]

[tasks.clippy-fix]
description = "Fix linting issues automatically"
command = "cargo"
args = ["clippy", "--all-features", "--fix", "--allow-dirty", "--allow-staged"]

[tasks.quality]
description = "Run all code quality checks (format + lint)"
dependencies = ["fmt-check", "clippy-check"]

[tasks.quality-fix]
description = "Fix all code quality issues automatically"
dependencies = ["fmt-fix", "clippy-fix"]

# ============================================================================
# Build & Clean
# ============================================================================

[tasks.build]
description = "Build the project in debug mode"
command = "cargo"
args = ["build", "--all-features"]

[tasks.build-release]
description = "Build the project in release mode"
command = "cargo"
args = ["build", "--release", "--all-features"]

[tasks.clean]
description = "Clean build artifacts"
command = "cargo"
args = ["clean"]

# ============================================================================
# WASM Testing Tasks
# ============================================================================

# `--no-default-features` is forwarded to the underlying `cargo build --tests
# --target wasm32-unknown-unknown` so the `with-reinhardt`-gated `manage` bin
# (native-only, uses tokio / reinhardt::commands / async fn main) is skipped
# via its `required-features`. Without this flag the wasm test build pulls
# the bin in and fails to compile.
#
# `--features client-router` is also forwarded so that the
# `#[cfg(feature = "client-router")]`-gated `impl ClientUrlResolver for
# ResolvedUrls` block emitted by the `#[routes]` macro (see
# `crates/reinhardt-core/macros/src/routes_registration.rs` around the
# `ResolvedUrls` definition) stays in scope. Without it, `links.rs` cannot
# call `resolve_client_url(name, params)` because the trait impl is gated
# out — `--no-default-features` alone disables the example's default
# `client-router` feature flag, which is what the macro probes at expansion
# time in the consumer crate.
[tasks.wasm-test]
description = "Run WASM tests in headless Chrome"
command = "wasm-pack"
args = ["test", "--headless", "--chrome", "--", "--no-default-features", "--features", "client-router"]

[tasks.wasm-compile-dev]
description = "Compile WASM binary (debug mode)"
command = "cargo"
args = ["build", "--target", "wasm32-unknown-unknown"]

[tasks.wasm-bindgen-dev]
description = "Generate WASM bindings using wasm-pack (debug mode)"
dependencies = ["wasm-compile-dev"]
command = "wasm-pack"
args = [
	"build",
	"--target", "web",
	"--out-dir", "dist-wasm",
	"--dev",
	"--no-typescript"
]

[tasks.wasm-build-dev]
description = "Build WASM in debug mode and collect static files"
dependencies = ["wasm-bindgen-dev"]
script = { file = "scripts/wasm-build-dev.sh" }

[tasks.wasm-compile-release]
description = "Compile WASM binary (release mode)"
command = "cargo"
args = ["build", "--target", "wasm32-unknown-unknown", "--release"]

[tasks.wasm-bindgen-release]
description = "Generate WASM bindings using wasm-pack (release mode)"
dependencies = ["wasm-compile-release"]
command = "wasm-pack"
args = [
	"build",
	"--target", "web",
	"--out-dir", "dist-wasm",
	"--release"
]

[tasks.wasm-finalize-release]
description = "Optimize WASM with wasm-opt and collect static files"
dependencies = ["wasm-bindgen-release"]
script = { file = "scripts/wasm-finalize-release.sh" }

[tasks.wasm-build-release]
description = "Build WASM in release mode with optimization and collect static files"
dependencies = ["wasm-finalize-release"]
script = { file = "scripts/wasm-build-release.sh" }

# `wasm-clean` was removed — `clean-cache` (below) already removes the
# same `dist/` + `dist-wasm/` directories and additionally drops the Rust
# incremental cache that drives the next `cargo build`, so the WASM-only
# variant became redundant.

[tasks.clean-cache]
description = "Clean WASM artifacts and Rust incremental build cache"
script = { file = "scripts/clean-cache.sh" }

# ============================================================================
# Development Workflow
# ============================================================================

[tasks.dev]
description = "Build WASM and start development server with frontend (auto-starts PostgreSQL/Redis via `migrate` → `infra-up`)"
dependencies = ["wasm-build-dev", "migrate", "run-dev-server"]

[tasks.run-dev-server]
description = "Start development server with frontend (MUST run from project directory)"
script = { file = "scripts/run-dev-server.sh" }

[tasks.dev-release]
description = "Build optimized WASM and start server (auto-starts PostgreSQL/Redis via `migrate` → `infra-up`)"
dependencies = ["wasm-build-release", "collectstatic", "migrate", "run-dev-release-server"]

[tasks.run-dev-release-server]
description = "Start release-mode dev server against the optimised WASM bundle produced by `wasm-build-release`"
script = { file = "scripts/run-dev-release-server.sh" }

# ============================================================================
# CI/CD Workflow
# ============================================================================

[tasks.ci]
description = "Run CI pipeline (format, lint, build, test)"
dependencies = ["fmt-check", "clippy-check", "build", "test"]
