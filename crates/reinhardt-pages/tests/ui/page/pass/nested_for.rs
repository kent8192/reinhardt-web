//! page! macro with nested for loops

use reinhardt_pages::page;

fn main() {
	// Simple for loop
	let _simple = __reinhardt_placeholder__!(/*0*/);

	// For loop with nested if
	let _for_if = __reinhardt_placeholder__!(/*1*/);
}
