# Reinhardt Event Catalog

`reinhardt-event-catalog` is Reinhardt's dependency-free source of truth for
standard HTML and SVG element event names, payload names, browser interface
families, capabilities, and native dispatch defaults.

Runtime, parser, macro, and public payload crates consume this metadata instead
of maintaining separate event tables.
