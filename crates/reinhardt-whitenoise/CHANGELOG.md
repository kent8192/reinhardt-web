# Changelog

All notable changes to `reinhardt-whitenoise` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial crate structure
- `WhiteNoiseConfig` with builder pattern
- `FileMetadata` with ETag generation
- `CompressedVariants` for tracking compressed files
- `FileCache` for O(1) metadata lookup
- Immutable file detection with default regex pattern
- Basic error types

### Changed

### Deprecated

### Removed

### Fixed

### Security

## [0.1.0] - TBD

Initial release (planned)
